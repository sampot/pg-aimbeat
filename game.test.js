import{describe,it,expect}from"vitest";import{newGame,quality,hit,miss}from"./game.js";describe("準星節拍",()=>{it("完美判定在拍點附近",()=>{expect(quality(1000,1008)).toBe("perfect");expect(quality(1000,1120)).toBe("good");expect(quality(1000,1300)).toBe("miss")});it("命中增加分數與連擊",()=>{const g=newGame();expect(hit(g,1000,1030)).toBe("perfect");expect(g.combo).toBe(1);expect(g.score).toBe(300)});it("失誤中斷連擊",()=>{const g=newGame();g.combo=4;miss(g);expect(g.combo).toBe(0)})});